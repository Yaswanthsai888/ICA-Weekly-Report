# Copy this file to .env and adjust values before starting in production

# Port the server listens on (default: 5000)
PORT=5000

# Set to 'production' to serve the React build and disable CORS
NODE_ENV=production
